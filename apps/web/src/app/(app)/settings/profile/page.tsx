import { ProfileSettings } from "@/components/settings/ProfileSettings";

// Profile & account settings — photo, name, appearance (theme), and password.
// Reached from the profile menu (sidebar bottom on desktop, header on mobile).
export default function ProfileSettingsRoute() {
  return <ProfileSettings />;
}
